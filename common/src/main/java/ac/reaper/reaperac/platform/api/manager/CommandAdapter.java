package ac.reaper.reaperac.platform.api.manager;

/**
 * Marker interface for platform-specific command adapter implementations.
 * Implement this on adapter types that are injected into command registration wiring.
 */
public interface CommandAdapter {
}
